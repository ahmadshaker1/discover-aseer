/** Shared props for sidebar / filter SVG icons — use with `currentColor` fills/strokes. */
export type FilterIconProps = {
  className?: string;
};

/** Section header icons (heart, wallet, clock, etc.) */
export const filterSectionIconClass = "shrink-0 text-muted-foreground";

/** Icons inside controls that inherit label color (city dropdown, chevrons) */
export const filterControlIconClass = "shrink-0";
